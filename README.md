Краткое описание проекта. Цель проекта, проанализировав учащихся двух вузов. По направлению програмирования, я пришел к выводу, что большинство не знает иностранного языка, чтобы это исправить я принял решение написать телеграм бота, который поможет студентам в изучениии анлийского языка. Проект нацелен в первую очередь на то чтобы пользователь в первую очередь запонимал и вписывал новые для себя слова в телеграм бота, в свою очерель бот будет сможет проверить пользователя в виде "викторины" или "теста"

Команды для использования бота 

📚 Доступные команды:
/start - Начать
/add_word - Добавить слово
/my_words - Мои слова
/practice - Практика
/quiz - Викторина
/help - Помощь


Структуры проекта
english-learning-bot/
├── src/
│   ├── __init__.py
│   ├── bot.py              # Ваш текущий код бота
│   ├── data_handler.py     # Класс UserData выделен в отдельный модуль
│   └── config.py           # Конфигурация (токен, настройки)
├── tests/
│   └── __init__.py
├── data/
│   └── words.json          # Файл с данными (создаётся автоматически)
├── Dockerfile
├── docker-compose.yml
├── .dockerignore
├── .gitignore
├── .pre-commit-config.yaml
├── requirements.txt
├── requirements-dev.txt
├── .env.example
├── README.md
└── pyproject.toml
Запуск локально.
клонирование репозитория
git clone <ваш-репозиторий>
cd english-learning-bot
установка зависимостей
pip install -r requirements.txt
requirements.txt должен содержать 
aiogram==3.23.0
python-dotenv==1.0.0
python--version 3.14.0


Пример использования HTTP
# 1. Получить статистику
curl -X GET "http://localhost:8000/stats"

# Ответ:
{
  "total_users": 15,
  "total_words": 127,
  "avg_words_per_user": 8.47
}

# 2. Получить слова пользователя
curl -X GET "http://localhost:8000/users/123456789/words"

# Ответ:
{
  "apple": "яблоко",
  "house": "дом",
  "book": "книга"
}

CLI пример
команды 
 Просмотр статистики
python bot.py --stats

# Экспорт слов пользователя
python bot.py --export-words --user-id 123456789 --format json

# Импорт слов
python bot.py --import-words --file words.csv

# Резервное копирование
python bot.py --backup --output backup.zip

# Восстановление
python bot.py --restore --file backup.zip
